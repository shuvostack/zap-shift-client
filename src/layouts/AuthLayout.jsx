import React from 'react';
import Logo from '../components/Logo/Logo';

const AuthLayout = () => {
    return (
        <div>
            <Logo></Logo>
        </div>
    );
};

export default AuthLayout;